# Token 修改日志

记录我们在这个工作目录中对 Token Monitor 做的实际修改。按本地日期（Asia/Shanghai）追加；只记已做、已验证的事，不记录账号凭证、Cookie 或私人数据。未提交的改动也要明确标注，避免把工作目录状态误认为 Git 历史。

## 2026-09-23

- 将“音乐”放在左下角视图菜单的第一项，并把音乐页嵌入 Token 原窗口，不再另开播放器窗口；切换回统计视图时保留网页，以便继续播放。加入收藏页、返回、上一首、播放／暂停、下一首和刷新控件。涉及 `src/electron/musicPlayer.js`、主进程／预加载接线、渲染页面、样式与文案。
- 早期独立播放器实验曾保留在工作目录中；后续已移出仓库，浏览器会话缓存未纳入 Git。
- 验证：`npm run lint` 通过；音乐页与视图菜单定向测试通过；`git diff --check` 通过。`npm run verify` 的完整测试在监听器相关用例失败后被中止，不能记作通过；失败发生在 `tests/shared/collectorLoadGuards.test.js`，与本次音乐改动无直接关联。
- 已彻底退出旧 Token 进程并重新启动；新主进程正常运行。内嵌页的实际显示效果、登录状态和播放操作仍待在界面中确认。
- Git 状态：工作在 `maintenance/judy-widget` 分支；上述音乐改动及实验目录仍未提交、未推送。
- 修复切换 Codex 账号后的今日统计下降：逐会话归档不再用较小的同会话快照覆盖已记录用量；实时快照读到较小值时按会话差额补回，不重复计算整条会话。对旧逻辑已覆盖、无法还原到具体会话的今日差额，以此前实际显示过的今日 Codex 峰值作为下限，并标记为“此前已记录、未归属到具体会话”的用量；不伪造账号归属或费用。
- 验证：归档、SQLite 存储、项目汇总和音乐相关定向测试共 73 项通过；`npm run lint -- --quiet`、`git diff --check` 通过。完整测试此前有无关监听器测试失败，此次未重复宣称整套通过。
- 已完整退出并重新启动 Token；新主进程正常运行。今日历史快照仍保留此前显示的 Codex 用量，实际窗口数字待界面确认。
- 将音乐页改为 Token 原尺寸内的迷你播放器：前台只显示封面、歌曲信息、进度、播放控制和收藏列表；B 站网页留在隐藏的持久会话中播放，不再露出完整网页。收藏列表由 B 站公开只读接口提供，支持选曲和继续加载；视频 URL 请求 360P，并在页面播放器可用时尝试切换，界面只在确认后显示“360P”，否则显示“360P 优先”。
- 验证：音乐、视图菜单与会话归档相关 68 项定向测试通过；lint 和差异检查通过。实际 B 站播放及画质仍待界面验证。

## 2026-09-24

- 根据实机截图重做音乐页外观：改为更紧凑的深色唱片风格、红色播放强调，并压缩小窗口布局；列表保留独立滚动，底部控件不再被挤出。
- 修复封面和列表预览图：Token 原 CSP 只允许本地图片，现仅放行 B 站图片 CDN `https://*.hdslb.com`；图片失败时回退占位，不扩大脚本或网络连接权限。
- 记住最后选中的歌曲 BVID，保存在应用本机数据目录的 `music-player.json`；再次打开音乐页或重启 Token 后尝试恢复并自动播放。首次使用没有历史歌曲时仍需选一首；视频未就绪会短时间重试。
- 验证：音乐、视图和会话归档相关 99 项定向测试通过；lint、语法和 `git diff --check` 通过。实际 B 站播放、封面及小窗口视觉仍需用户界面确认。改动尚未提交。

- 加入播放器音量滑杆、分 P 下拉选择；视频播完时先播放下一 P，最后一 P 才跳到收藏列表下一首。离开音乐页后，Token 底部显示当前歌曲摘要，点击可返回播放器。
- 将音量条收进“下一首”右侧的竖向弹层，底部歌曲摘要改为透明背景并沿用主题文字色。播放器状态增加来源描述，曲目向 UI 提供通用 `id` 与 `sourceId`，为后续平台接入保留同一界面契约；当前实际接入仍只有 B 站。
- 将播放器控制组向右调整，使暂停键与封面／标题中线对齐；分 P 从会弹出窗口外的原生下拉框改成 Token 内独立、可滚动的选择页，支持返回播放器。
- 尝试为底部音乐摘要加入同步歌词：只读取 B 站公开字幕元数据与字幕 JSON，不把弹幕或画面内嵌字当歌词；按播放时间显示当前一句，长句在第二行限定区域内滚动，无字幕时仍只显示歌名。实测最近视频的公开字幕列表为空，因此该首不会显示歌词。
- 添加顶栏 B 站全站搜歌入口与收藏列表内的关键词搜索；两种结果独立展示、可直接点播，全站结果不会混进收藏列表。B 站公开收藏接口的 `keyword` 可搜索尚未加载的收藏。重启后恢复上次歌曲（包括非收藏搜索结果）的信息，但保持暂停；点播放才开始，播放器尚未就绪时会等它加载。
- 验证：音乐与相邻功能的 107 项定向测试、lint、`git diff --check` 通过。完整 `npm run verify` 的 lint 通过，但全量测试在此前已知的 `tests/shared/collectorLoadGuards.test.js` 监听器用例失败后未正常结束，本次主动中止；不能宣称整套通过。实际搜索点播和重启后的暂停状态仍待窗口操作确认。改动未提交。
- 优化恢复速度：优先从本机已保存的歌曲元数据载入暂停状态，收藏列表再后台读取；若该歌仍在收藏中，随后把播放队列接回收藏，上一首／下一首继续可用。
- 修复播放状态误报：以前 `play()` 的拒绝被吞掉，界面会一直显示“正在尝试播放”；现在等播放 Promise 真正成功才显示“正在播放”，失败显示具体原因，并区分媒体网络、解码或格式错误。实测当前视频页、元数据、搜索和播放流的公开接口均返回 HTTP 200，未见总体网络故障。
- 清理旧独立播放器实验（含浏览器会话缓存）：已移到 `/tmp` 下仅本机可访问的临时备份，不会上传；正式代码不再加载无用的收藏网页。个人收藏夹 ID 已迁入 Git 忽略的本机 `.env`，仓库只保留配置入口。
- 最终检查：相关定向测试和 lint 通过；除已知独立失败的 `collectorLoadGuards.test.js` 外，其余测试文件低并发运行全部通过。该监听器用例单独运行仍失败，且涉及文件本轮未改；完整 `npm run verify` 不能记作通过。待上传差异已检查个人标识与本机配置。

- 优化音乐页与搜索：移除遮挡自定义背景的不透明底板，改为轻量玻璃层；调整搜索图标、控件悬停／按下反馈、输入框和结果列表。搜索请求改用已登录播放器的 Chromium 会话，HTTP 412 改为可理解的提示；输入法组词期间不触发搜索，搜索框使用普通文本模式。
- 验证：音乐页、基础样式和动效相关 36 项测试通过；lint 与差异格式检查通过。Token 已完全退出并重新启动。WSLg 的 X11 窗口未安装 Linux 中文输入法，Windows 输入法是否能直接上屏仍需实机确认，不能把 DOM 事件修复等同于系统输入法修复。

- 为 WSLg 版音乐搜索增加“中文输入”入口：Windows 原生置顶输入框接收 Windows 输入法文字，返回后回填搜索框并搜索；全站和收藏搜索都可用。其他平台不显示该入口，普通输入与粘贴不受影响。启动脚本不拼接用户输入，结果通过 UTF-8 Base64 回传；弹窗取消不改搜索内容。
- 验证：Windows 表单自动关闭联通测试实际从 WSL 返回“中文”；音乐页与基础样式 27 项测试、lint 通过。尚需用户亲手用 Windows 拼音输入确认候选字上屏和弹窗前后层级。改动仍在本地，未提交或上传。

- 修复 desktop（固定在桌面）模式无法用鼠标边框缩放：此前该模式同时将窗口设为不可缩放，并隐藏 WSLg 的八个边缘手柄；现在保留桌面层级，但允许原生缩放和边缘手柄。更新五种语言的模式说明，并加回归测试。窗口模式、缩放和基础样式 20 项测试、lint 通过；重启后仍需在实际窗口用鼠标确认边缘拖动手感。改动未提交或上传。

- 按实际使用反馈调整音乐控制区：播放／暂停从字体字符改为一致的 SVG 图标，中心按钮改用更轻的浅色表面与红色图标，保留悬停／按下反馈。
- 移除单独的“中文输入”按钮。WSLg 上点搜索图标或已打开的搜索框即调用 Windows 原生输入框；上次关键词预填并全选，确认后回填并搜索。Windows IME 仍无法直接在 WSLg 的 Chromium 输入框内组词，此改动将操作压缩为一次点击，不宣称解决系统级输入法转发。其他平台仍直接聚焦普通输入框。
- 验证：音乐和基础样式 28 项测试、lint 通过。实际播放键视觉与 Windows 拼音候选框仍待用户在当前窗口确认；本轮改动未提交或上传。

- 2026-09-24：为 WSLg 启动器配置独立 D-Bus 会话与 Fcitx5 拼音，让 Token 音乐搜索使用页面内输入框；移除 Windows 原生输入弹窗和对应测试，保留拼音组合输入结束后触发搜索。

- 2026-09-24：音乐搜索加入 B 站结果翻页与“加载更多”（含收藏内搜索、去重和旧请求防串结果）；进度条拖动时保留本地预览并平滑推进时间，播放/暂停即时反馈；切歌与切分 P 时从 0 开始，仅在重启时恢复上次选择的歌曲、保持暂停。

- 2026-09-24：音乐模式标题栏左上角增加返回 Token 按钮，仅音乐模式显示；点击关闭音乐视图并恢复原统计界面，不暂停后台歌曲。

- 2026-09-24：返回 Token 统计界面后，底部歌名超出可用宽度时从右向左循环滚动；短歌名静止、悬停暂停、窗口宽度变化重算，系统减少动态效果时保留省略号。歌词继续独立占第二行。

- 2026-09-24：修复 Token 重启后底栏不显示上次歌名：启动时仅从本机已保存的歌曲元数据恢复暂停态底栏，不打开 B 站页面；用户再点底栏时仍按原逻辑打开播放器。

- 2026-09-24：按播放状态控制底栏动态效果：播放时长、短歌名都从右向左滚动；暂停时歌名和歌词立即静止，长歌名回到开头并省略显示；移除悬停暂停滚动，避免滚动状态误导播放状态。系统减少动态效果仍保留静态显示。

- 2026-09-24：音乐页隐藏无关的 DAY/MONTH/TOTAL 周期标签（窗口控制保留）；返回键移到音乐卡片左上、替代红色音符；来源名改为小写 bilibili；上一首、播放/暂停、下一首、音量四个图标及点击区域稍放大。

- 2026-09-24：修正中央播放键三角形在圆形按钮内偏右：删除额外水平位移并清除按钮默认内边距，播放／暂停图标统一按网格中心对齐。

- 2026-09-24：音乐主界面在歌曲信息／分 P 与进度条之间加入当前字幕歌词单行；有歌词且正在播放时从右向左循环滚动，暂停则静止，无歌词不占空间。底部原有歌词保留。

- 2026-09-24：音乐界面支持局部空格播放／暂停：仅 Token 窗口有焦点且鼠标位于 Token 上、无输入框编辑或输入法组词时生效，不注册全局快捷键。字幕 API 请求改走已登录 B 站播放会话（无会话时保留公开请求）；当前公开测试视频的字幕列表为空，不能保证该视频有歌词。

- 2026-09-24：修复 B 站自行连播导致 Token 曲目元数据停留上一首：除 video.ended 外，识别视频地址变更与临近末尾后进度归零；统一切到 Token 自己队列的下一首/下一 P，列表结束时暂停，旧轮询不能覆盖新曲目状态。手动拖动进度短暂屏蔽归零判定。

- 2026-09-24：音乐页字幕改为前句／当前句／后句三行竖向显示，当前句按 B 站字幕时间戳切换；逐句前进有轻量下移过渡，无字幕则隐藏。轮询从 1.2 秒缩短到 0.4 秒并避免轮询重叠；进度条改为小数秒精度，拖动后等待后台确认，防止旧采样将位置拉回。仍受视频是否提供准确时间轴字幕限制。

- 2026-09-24：打开“我的收藏”列表时滚动定位当前播放歌曲，使它成为列表首个可见项；当前歌曲高亮。列表尚在加载时，完成后补定位一次；后续分页保留手动滚动位置，不反复跳回当前歌曲。

- 2026-09-24：修复逐句歌词切换时播放器内容“挤一下”、进度条位置跳动：有字幕的歌曲始终保留固定高度歌词区，字幕句间空档不再把整个区域隐藏；过渡只作用于当前句，移动距离缩小到 3px，前后句和进度条保持原位。

- 2026-09-24：对照用户使用的 vCaptions（原“哔哔君”）公开源码，确认其优先从已登录 B 站会话请求 `x/player/wbi/v2` 字幕轨道；Token 改为同路径优先、原 `x/player/v2` 回退。字幕选轨优先人工中文，其次 AI 中文，不再误选任意外语轨道；无字幕地址或失败轨道会尝试回退。截图对应《六月的雨》（BV1vo4y1M7kW）的未登录接口返回空轨道，插件登录态显示逐句歌词；登录会话差异是待在 Token 内确认的可能因素。

- 2026-09-24：参考网易云的逐行歌词观看体验（非复制其内部实现），将旧的“先替换文字再让当前行抖动”改为固定 54px 视口内四行歌词列连续位移一行，当前行高亮同步过渡；动画结束后无缝复位。字幕间短空档保留上一句，减少动态效果模式及跳播时立即切换；进度条所在布局不参与动画。

- 2026-09-24：按反馈移除 Token 底部音乐栏的歌词显示，仅保留歌曲名与当前分 P 标题；打开分 P 列表时自动滚动定位并高亮当前分 P，避免默认停在第一项。

- 2026-09-24：修复 B 站播放器内部自动切换分 P 后 Token 状态不跟随：轮询读取当前 URL 的 p 参数，发现分 P 变化时同步 partIndex、清空旧歌词、重新加载对应 CID 字幕并刷新列表高亮与定位。

- 2026-09-24：底部播放信息在分 P 视频中改为显示当前分 P 的具体歌名，不再重复显示总合集标题；无分 P 或分 P 标题缺失时才回退到总标题。
